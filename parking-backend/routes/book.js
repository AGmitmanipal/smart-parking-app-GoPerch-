const express = require("express");
const router = express.Router();

// Routes have been deprecated/migrated to reserve.js
// This file is kept to prevent import errors in server.js but all routes are removed or return 404.

module.exports = router;
